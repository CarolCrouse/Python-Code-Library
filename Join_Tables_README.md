This code uses ArcPy to doing the following:

Within a gdb it adds a new field to a layer.
It then adds/joins user specified fields from a 2nd layer within the same gdb, based on a unique join field.
